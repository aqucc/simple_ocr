<div align="center">

# 使い方マニュアル

画面ごとの操作ガイド。機能の一覧は [`FEATURES.md`](./FEATURES.md)、内部仕様は [`SPEC.md`](./SPEC.md)。

</div>

---

## はじめに

[アプリ](https://aqucc.github.io/simple_ocr/) をブラウザで開くだけで使えます。インストール不要、画像は端末から外に出ません。初回の読み取り時に OCR エンジン（数十MB）を取得するため、最初だけ少し待ちます。

上部のタブで **撮影** と **一覧** を切り替えます。

---

## 撮影 → 読み取り

### 1. 画像を入力する

| ボタン | 動作 |
| --- | --- |
| カメラで撮影 | カメラ／写真ライブラリを開く |
| 画像を選択 | 端末内の画像ファイルを選ぶ |
| クリップボードから貼り付け | コピー済みの画像を貼り付け（`Ctrl` / `Cmd + V` でも可） |

> **認識モード**を先に選びます。型番・製造番号など半角英数字だけなら **英数字(型番向け)**、住所や品名など和文を含むなら **日本語+英数字**。選択は次回も記憶されます。

### 2. 範囲を指定して読み取る

画像を読み込むと切り抜き画面になります。

- **矩形モード**（既定）— 枠をドラッグで移動、四隅で拡縮。**読みたい行だけを囲む**と精度が上がります。
- **台形補正モード** — 斜めから撮った写真向け。四隅を独立に動かしてラベルの角に合わせると、正面から撮ったように補正します。

読み取りボタン:

- **この範囲を読み取る** — 枠内だけを認識（推奨）
- **全体を読み取る** — 画像全体から文字行を自動検出して認識
- **やり直す** — 入力に戻る

### 3. 確認して保存

認識結果がテキストボックスに出ます。

- そのまま編集できます。
- **単語フィルタ**（テキスト上部）— ノイズや記号の誤読を除くフィルタ。オフにすると生の認識結果に戻せます。
- **保存する** で記録。**範囲を選び直す** で切り抜きへ戻れます。

---

## 一覧・カレンダー

保存した記録は日付ごとにまとまって並びます。

- **カレンダー** — 記録がある日にバッジ。日付をタップでその日だけ表示、「すべて表示」で解除。
- **サムネイル** — タップで全画面拡大（どこかをタップで閉じる）。
- 各記録の操作:

| ボタン | 動作 |
| --- | --- |
| コピー | テキストをクリップボードへ |
| 再読み取り | 保存画像を撮影フローに戻して読み直す |
| 編集 | テキストをその場で修正して保存 |
| 削除 | 確認のうえ削除 |

---

## 困ったとき

| 症状 | 対処 |
| --- | --- |
| 古い画面が出る | 再読み込み、またはプライベートタブで開く（ヘッダ右の英数字が公開バージョン） |
| 貼り付けボタンが効かない | HTTP(S) 配信が必要（`file://` では不可）。`Ctrl`/`Cmd + V` を試す |
| 日本語の精度がいまひとつ | 読みたい行だけを枠で囲む／型番のみなら英数字モード |
| 斜めで読めない | 台形補正モードで四隅を合わせる |
| 保存記録が消えた | ブラウザのサイトデータ削除で消えます（端末内保存のため） |
